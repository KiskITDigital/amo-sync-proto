syntax = "proto3";

package models.v1;

import "models/v1/contact.proto";

message CreateLead {
    reserved 1; 
    optional models.v1.Contact contact = 2;
    optional string contact_external_id = 3;
    optional string company_external_id = 4;
    optional string initial_message = 5;
}